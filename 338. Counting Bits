class Solution {
    public int[] countBits(int num) {
        int[] ans = new int[num+1];
        if(num==0) return ans;
        ans[1] = 1;
        for(int i = 2;i < ans.length;i++) {
            ans[i] = ans[i>>1] + ( i&1);
        }
        return ans;
    }
}
